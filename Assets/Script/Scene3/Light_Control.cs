using System.Collections;
using System.Collections.Generic;
using UnityEngine;

public class Light_Control : MonoBehaviour
{
    // Start is called before the first frame update
    GameObject flashlight;
    GameObject flashlight_out;
    GameObject Lights; // all light effects and spotlight
    AudioSource switch_sound; // audio of the switcher
    ParticleSystem dust_particles; // dust particles


    //girl.GetComponent<Transform>().position;
    void Start()
    {
        
    }

    // Update is called once per frame
    void Update()
    {
        
    }
}
